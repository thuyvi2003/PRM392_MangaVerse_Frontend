<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MangaVerse"
        tools:targetApi="31">
        
        <!-- Onboarding Activity - Launcher -->
        <activity
            android:name=".OnboardingActivity"
            android:exported="true"
            android:theme="@style/Theme.MangaVerse">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Auth Activity -->
        <activity
            android:name=".AuthActivity"
            android:exported="false"
            android:theme="@style/Theme.MangaVerse" />
        
        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.MangaVerse" />
        
        <!-- Discover Activity -->
        <activity
            android:name=".DiscoverActivity"
            android:exported="false"
            android:theme="@style/Theme.MangaVerse" />
        
        <!-- Manga Reader Activity -->
        <activity
            android:name=".MangaReaderActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:configChanges="orientation|screenSize" />
        
        <!-- Settings Activity -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="true"
            android:theme="@style/Theme.MangaVerse" />

        <!-- Profile Activity -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.MangaVerse" />
        
        <!-- Home Activity -->
        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:theme="@style/Theme.MangaVerse.NoActionBar" />
        
        <!-- My Books Activity -->
        <activity
            android:name=".MyBooksActivity"
            android:exported="false"
            android:theme="@style/Theme.MangaVerse.NoActionBar" />
        
        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.MangaVerse.WithActionBar" />
    </application>

</manifest>